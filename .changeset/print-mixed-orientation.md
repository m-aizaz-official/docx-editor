---
'@docx-editor.dev/react': patch
---

Fix print output shrinking and left-aligning pages when a document mixes portrait and landscape sections. Each page now prints on paper matching its own dimensions instead of being fit to the widest page.
